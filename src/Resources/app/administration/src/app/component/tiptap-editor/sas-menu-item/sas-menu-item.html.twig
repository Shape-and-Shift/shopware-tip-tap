<div class="sas-menu-item">
    <button
        class="menu-item"
        :class="{ 'is-active': isActive ? isActive(): null }"
        @click="action"
        :title="title"
    >

        <svg class="remix">
            <use :xlink:href="`/sastiptapeditor/static/img/remixicon.symbol.svg#ri-${icon}` | asset" />
        </svg>
    </button>
</div>
